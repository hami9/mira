# CLAUDE.md

راهنمای کامل کار روی این پروژه در **[AGENTS.md](AGENTS.md)** است.

پیش از هر تغییری آن را کامل بخوان — شامل معماری، تصمیم‌های طراحی و دلیلشان،
باگ‌های واقعی که قبلاً پیدا و رفع شده‌اند، تله‌های محیط توسعه (Windows + Docker)،
و کارهای تست‌نشده‌ی باقی‌مانده.

خلاصه‌ی چند نکته‌ی حیاتی:

- زبان پروژه فارسی است (کامنت، UI، پیام کامیت)؛ نام‌های کد انگلیسی.
- `synchronize` هیچ‌وقت روشن نمی‌شود — هر تغییر اسکیما یک مایگریشن جدید می‌خواهد.
- هر کوئری باید با `siteId` فیلتر شود (سیستم چندمستأجری).
- فراخوانی AI هرگز روی مسیر درخواست نیست، همیشه در صف BullMQ.
- با سرویس واقعی تست کن، نه با بازبینی کد — تقریباً همه‌ی باگ‌های واقعی این پروژه
  فقط با اجرای واقعی پیدا شدند.
- بعد از هر کار: کامیت و پوش.
