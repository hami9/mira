{
  "name": "@mira/widget",
  "version": "1.0.0",
  "private": true,
  "description": "ویجت جاسازی‌شونده در سایت مشتری، TypeScript خالص بدون فریمورک",
  "scripts": {
    "dev": "node esbuild.config.mjs --watch",
    "build": "node esbuild.config.mjs"
  },
  "dependencies": {
    "@mira/shared-types": "*",
    "socket.io-client": "^4.8.0"
  },
  "devDependencies": {
    "esbuild": "^0.23.1"
  }
}
