#!/bin/sh
# پیش از حذف پکیج mira — سرویس متوقف می‌شود ولی داده‌ها (volumeهای داکر و /etc/mira) می‌مانند
set -e

case "$1" in
  remove)
    systemctl stop mira >/dev/null 2>&1 || true
    systemctl disable mira >/dev/null 2>&1 || true
    ;;
esac

exit 0
